name: Publish Self Esteem Linter

on:
  push:
    tags:
      - 'self-esteem-linter/v*'

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: extensions/self-esteem-linter
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
          cache-dependency-path: extensions/self-esteem-linter/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: xvfb-run -a npm test --if-present

      - name: Build extension
        run: |
          npm run build --if-present
          npm run package --if-present

      - name: Publish to Marketplace
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}
        run: npx @vscode/vsce publish
